package queue

/*import (
	"github.com/hibiken/asynq"
)

type RedisService struct {
	client *asynq.Client
}

func InitRedisService(host string, password string, db int) Maker {
	redisClient := asynq.NewClient(asynq.RedisClientOpt{
		Addr:     host,
		Password: password,
		DB:       db,
	})

	return &RedisService{client: redisClient}
}

func (service *RedisService) NewTask(uid int, payload *Payload) error {
	//
	return nil
}
*/
